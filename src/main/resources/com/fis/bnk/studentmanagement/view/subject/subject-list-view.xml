<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
  xmlns:c="http://jmix.io/schema/flowui/jpql-condition"
  title="msg://subjectListView.title"
  focusComponent="subjectsDataGrid">
  <data>
    <collection id="subjectsDc"
      class="com.fis.bnk.studentmanagement.entity.Subject">
      <fetchPlan extends="_base"/>
      <loader id="subjectsDl">
        <query>
          <![CDATA[select s from Subject s]]>
          <condition>
            <and>
              <c:jpql>
                <c:where>lower(s.code) like :code</c:where>
              </c:jpql>
              <c:jpql>
                <c:where>lower(s.name) like :name</c:where>
              </c:jpql>
            </and>
          </condition>
        </query>
      </loader>
    </collection>
  </data>
  <facets>
    <dataLoadCoordinator auto="true"/>
    <urlQueryParameters>
      <pagination component="pagination"/>
    </urlQueryParameters>
  </facets>
  <actions>
    <action id="selectAction" type="lookup_select"/>
    <action id="discardAction" type="lookup_discard"/>
  </actions>
  <layout classNames="form-container">
    <!-- Add search fields for Code, Name, Address, Phone Number -->
    <hbox spacing="true" classNames="form-container">
      <textField id="codeField" placeholder="Search by code" classNames="text-field"/>
      <textField id="nameField" placeholder="Search by name" classNames="text-field"/>
      <button id="searchBtn" text="Search" icon="SEARCH"/>
    </hbox>
    <hbox id="buttonsPanel" classNames="buttons-panel">
      <button id="createBtn" action="subjectsDataGrid.create"/>
      <button id="editBtn" action="subjectsDataGrid.edit"/>
      <button id="removeBtn" action="subjectsDataGrid.remove"/>
      <simplePagination id="pagination" dataLoader="subjectsDl"/>
    </hbox>
    <dataGrid id="subjectsDataGrid"
      width="100%"
      minHeight="20em"
      dataContainer="subjectsDc"
      columnReorderingAllowed="true">
      <actions>
        <action id="create" type="list_create"/>
        <action id="edit" type="list_edit"/>
        <action id="remove" type="list_remove"/>
      </actions>
      <columns resizable="true">
        <column property="code"/>
        <column property="name"/>
      </columns>
    </dataGrid>
    <hbox id="lookupActions" visible="false">
      <button id="selectBtn" action="selectAction"/>
      <button id="discardBtn" action="discardAction"/>
    </hbox>
  </layout>
</view>
