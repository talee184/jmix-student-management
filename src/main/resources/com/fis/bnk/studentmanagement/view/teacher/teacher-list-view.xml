<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
  xmlns:c="http://jmix.io/schema/flowui/jpql-condition"
  title="msg://teacherListView.title"
  focusComponent="teachersDataGrid">
  <data>
    <collection id="teachersDc"
      class="com.fis.bnk.studentmanagement.entity.Teacher">
      <fetchPlan extends="_base"/>
      <loader id="teachersDl">
        <query>
            <![CDATA[select t from Teacher t ]]>
            <condition>
              <and>
                <c:jpql>
                  <c:where>lower(t.name) like :name</c:where>
                </c:jpql>
                <c:jpql>
                  <c:where>t.age = :age</c:where>
                </c:jpql>
                <c:jpql>
                  <c:where>lower(t.address) like :address</c:where>
                </c:jpql>
                <c:jpql>
                  <c:where>t.phoneNumber like :phoneNumber</c:where>
                </c:jpql>
              </and>
            </condition>
        </query>
      </loader>
    </collection>
  </data>
  <facets>
    <dataLoadCoordinator auto="true"/>
    <urlQueryParameters>
      <pagination component="pagination"/>
    </urlQueryParameters>
  </facets>
  <actions>
    <action id="selectAction" type="lookup_select"/>
    <action id="discardAction" type="lookup_discard"/>
  </actions>
  <layout classNames="form-container">
    <!-- Add search fields for Code, Name, Address, Phone Number -->
    <hbox spacing="true" classNames="form-container">
      <textField id="nameField" placeholder="Search by name" classNames="text-field"/>
      <textField id="ageField" placeholder="Search by age" classNames="text-field"/>
      <textField id="addressField" placeholder="Search by address" classNames="text-field"/>
      <textField id="phoneNumberField" placeholder="Search by phone number" classNames="text-field"/>
      <button id="searchBtn" text="Search" icon="SEARCH"/>
    </hbox>
    <hbox id="buttonsPanel" classNames="buttons-panel">
      <button id="createBtn" action="teachersDataGrid.create"/>
      <button id="editBtn" action="teachersDataGrid.edit"/>
      <button id="removeBtn" action="teachersDataGrid.remove"/>
      <simplePagination id="pagination" dataLoader="teachersDl"/>
    </hbox>
    <dataGrid id="teachersDataGrid"
      width="100%"
      minHeight="20em"
      dataContainer="teachersDc"
      columnReorderingAllowed="true">
      <actions>
        <action id="create" type="list_create"/>
        <action id="edit" type="list_edit"/>
        <action id="remove" type="list_remove"/>
      </actions>
      <columns resizable="true">
        <column property="name"/>
        <column property="age"/>
        <column property="address"/>
        <column property="phoneNumber"/>
      </columns>
    </dataGrid>
    <hbox id="lookupActions" visible="false">
      <button id="selectBtn" action="selectAction"/>
      <button id="discardBtn" action="discardAction"/>
    </hbox>
  </layout>
</view>
